# BM25 关键词检索

BM25 是基于词项统计的稀疏检索算法。它考虑查询词在当前文档中的出现次数、该词在整个语料中的稀有程度，以及文档长度归一化。常见参数 k1 控制词频饱和速度，b 控制长度归一化强度。

BM25 擅长检索产品编号、接口路径、缩写、专有名词和错误码等精确词。例如查询 trace_id、ERR-1042 或 /health 时，关键词匹配通常比只依赖语义相似更稳定。

BM25 的限制是难以处理没有词面重合的同义表达。它适合作为混合检索的一路，而不是替代所有语义检索。

