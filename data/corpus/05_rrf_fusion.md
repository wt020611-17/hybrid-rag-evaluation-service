# RRF 与混合检索

不同检索器的原始分数通常不在同一尺度上。BM25 分数、余弦相似度和图路径分数不能在没有校准时直接相加，否则数值范围较大的一路会主导结果。

Reciprocal Rank Fusion（RRF）只使用文档在每一路中的名次。常用公式是 score(d) = sum(1 / (k + rank_i(d)))。同一文档被多路排在前面时会累积更高分；k 越大，各名次之间的差异越平缓。

RRF 的优点是简单、无需先校准异构分数；限制是忽略原始分数差距。项目必须同时保存各路排名和融合后排名，才能解释某条证据为什么进入 Top-K。

