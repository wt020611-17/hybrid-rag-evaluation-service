# 图检索与多跳路径

图检索把实体作为节点、关系作为边，用路径回答“谁依赖谁”“A 如何关联到 C”之类结构化问题。普通向量检索可以找相似文本，但不天然保证沿着明确关系完成多跳约束。

多跳并非越多越好。跳数增加会让候选路径快速膨胀，并放大实体或关系抽取错误。本项目限制最大深度为 2，并在每条结果中返回路径文本和 depth 元数据。生产系统还应增加关系白名单、方向约束、节点类型约束和超时。

图检索不应替代数据库 JOIN 或普通 RAG。字段完整、口径明确的结构化数据更适合 SQL；没有可靠实体关系时，强行建图只会增加维护成本。

