# RAG 基础链路

检索增强生成（RAG）把外部知识检索与语言模型生成组合起来。离线阶段负责加载文档、清洗、切分、生成索引并保存来源元数据；在线阶段接收查询，选择检索路线，召回证据，必要时重排，再把证据交给生成器。

RAG 的价值是让答案能够引用可更新的外部材料，并降低模型只依赖参数记忆造成的事实错误。它不能自动保证答案正确：如果切分破坏语义、索引过期、检索未命中或生成器忽略证据，结果仍然可能错误。

本项目把“检索正确”和“回答正确”分开观察。检索先用 Recall@K 与 MRR 验证，回答再检查引用是否支持结论。这样可以定位错误来自入库、检索、融合还是生成。

