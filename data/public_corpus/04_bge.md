# BGE-small-zh-v1.5 中文嵌入模型

来源：BAAI 模型卡（https://huggingface.co/BAAI/bge-small-zh-v1.5）。

BAAI/bge-small-zh-v1.5 是面向中文检索的文本嵌入模型，模型卡标注约 24M 参数、向量维度 512、最长序列 512，并采用 MIT 许可证。检索时可为查询添加中文检索指令，文档侧直接编码；向量归一化后用内积等价实现余弦相似度。生产实现应记录确切模型名、维度、池化与归一化方式，并把本地模型文件与代码版本对应，避免无意中切换模型导致索引失配。
