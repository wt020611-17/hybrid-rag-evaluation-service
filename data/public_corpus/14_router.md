# 可解释查询路由

来源：本项目工程设计，规则通过固定测试集验证。

查询路由用于减少无效通道并提供可解释决策。包含关系、路径、多跳等提示时选择 hybrid_graph；出现 API 名、错误码或版本号时偏向 BM25；为什么、含义、如何理解等概念问题偏向 BGE；其余问题走 BM25+BGE 混合检索。路由返回 decision 与 reason，并在评测集中保存 expected_route。规则路由不是学习模型，优点是可复现，缺点是覆盖有限，错误样本必须公开。
