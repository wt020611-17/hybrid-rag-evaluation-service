# Reports

`baseline.json` is generated by:

```powershell
$env:PYTHONPATH="src"
python -m hybrid_rag.cli eval --output reports/baseline.json
```

Do not edit metric values by hand. Commit the dataset, code version, backend names,
limitations, per-case results, and aggregate metrics together.

